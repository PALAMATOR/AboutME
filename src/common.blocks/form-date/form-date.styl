@import "../../library.blocks/mixins/container/container.styl"

.form-date
  {container}
  {container__column}
  &__row
    {container}
    @media screen and (max-width: 1024px)
      {container__column}
    justify-content start
